<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary
        xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

        <!-- Note: For Android please see also Platforms\Android\Resources\values\colors.xml -->

        <!-- Modern Blue Theme Palette -->

        <!-- Primary Colors -->
        <Color x:Key="Primary">#1E3A8A</Color>
        <!-- Deep Blue -->
        <Color x:Key="PrimaryDark">#1E40AF</Color>
        <!-- Slightly lighter Deep Blue for dark mode -->
        <Color x:Key="PrimaryDarkText">#DBEAFE</Color>
        <!-- Light Blue for text on dark -->
        <Color x:Key="Secondary">#3B82F6</Color>
        <!-- Sky Blue -->
        <Color x:Key="SecondaryDarkText">#60A5FA</Color>
        <!-- Lighter Sky Blue -->
        <Color x:Key="Tertiary">#DBEAFE</Color>
        <!-- Light Blue -->

        <!-- Neutral Colors -->
        <Color x:Key="White">#FFFFFF</Color>
        <Color x:Key="Black">#000000</Color>
        <Color x:Key="OffBlack">#1F1F1F</Color>

        <!-- Slate Gray Scale -->
        <Color x:Key="Gray100">#F1F5F9</Color>
        <!-- Light Gray -->
        <Color x:Key="Gray200">#E2E8F0</Color>
        <Color x:Key="Gray300">#CBD5E1</Color>
        <Color x:Key="Gray400">#94A3B8</Color>
        <Color x:Key="Gray500">#64748B</Color>
        <!-- Slate Gray -->
        <Color x:Key="Gray600">#475569</Color>
        <Color x:Key="Gray900">#0F172A</Color>
        <Color x:Key="Gray950">#020617</Color>

        <!-- Accent Colors -->
        <Color x:Key="Success">#10B981</Color>
        <!-- Emerald - for wins -->
        <Color x:Key="Warning">#F59E0B</Color>
        <!-- Amber - for highlights -->
        <Color x:Key="Error">#EF4444</Color>
        <!-- Rose - for losses -->
        <Color x:Key="Info">#3B82F6</Color>
        <!-- Sky Blue - for info -->

        <!-- Semantic Colors for Casino Theme -->
        <Color x:Key="CasinoGreen">#1B5E20</Color>
        <!-- Table felt green -->
        <Color x:Key="CasinoGold">#D4AF37</Color>
        <!-- Gold accents -->

        <!-- Brushes -->
        <SolidColorBrush x:Key="PrimaryBrush"
                         Color="{StaticResource Primary}"/>
        <SolidColorBrush x:Key="SecondaryBrush"
                         Color="{StaticResource Secondary}"/>
        <SolidColorBrush x:Key="TertiaryBrush"
                         Color="{StaticResource Tertiary}"/>
        <SolidColorBrush x:Key="WhiteBrush"
                         Color="{StaticResource White}"/>
        <SolidColorBrush x:Key="BlackBrush"
                         Color="{StaticResource Black}"/>

        <SolidColorBrush x:Key="Gray100Brush"
                         Color="{StaticResource Gray100}"/>
        <SolidColorBrush x:Key="Gray200Brush"
                         Color="{StaticResource Gray200}"/>
        <SolidColorBrush x:Key="Gray300Brush"
                         Color="{StaticResource Gray300}"/>
        <SolidColorBrush x:Key="Gray400Brush"
                         Color="{StaticResource Gray400}"/>
        <SolidColorBrush x:Key="Gray500Brush"
                         Color="{StaticResource Gray500}"/>
        <SolidColorBrush x:Key="Gray600Brush"
                         Color="{StaticResource Gray600}"/>
        <SolidColorBrush x:Key="Gray900Brush"
                         Color="{StaticResource Gray900}"/>
        <SolidColorBrush x:Key="Gray950Brush"
                         Color="{StaticResource Gray950}"/>

        <SolidColorBrush x:Key="SuccessBrush"
                         Color="{StaticResource Success}"/>
        <SolidColorBrush x:Key="WarningBrush"
                         Color="{StaticResource Warning}"/>
        <SolidColorBrush x:Key="ErrorBrush"
                         Color="{StaticResource Error}"/>
        <SolidColorBrush x:Key="InfoBrush"
                         Color="{StaticResource Info}"/>
</ResourceDictionary>
